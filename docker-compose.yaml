version: "3.8"

services:
  homeserver:
    image: homeserver:latest
    privileged: true
    cap_add:
      - NET_ADMIN
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: /init
    restart: unless-stopped
